% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/addVisits.R
\name{addVisits}
\alias{addVisits}
\title{Add Multi-Setting Visit Utilization Metrics to a Cohort}
\usage{
addVisits(
  x,
  indexDate = "cohort_start_date",
  censorDate = NULL,
  window = list(c(-365, -1), c(0, 365)),
  settings = c("inpatient", "outpatient", "emergency"),
  stratifySpecialty = TRUE,
  gpSpecialtyConceptIds = c(38004446L),
  icuSpecialtyConceptIds = c(38004500L),
  emergencySpecialtyConceptIds = c(38004510L),
  specialties = NULL,
  inpatientVisitConceptIds = c(9201L, 8717L, 581379L),
  outpatientVisitConceptIds = c(9202L, 581477L),
  emergencyVisitConceptIds = c(9203L, 262L, 581478L),
  icuConceptIds = 32037L,
  readmissions = FALSE,
  gapDays = 1L,
  collapseGap = NULL,
  countBy = c("days", "records"),
  collapseOverlapping = TRUE,
  name = NULL
)
}
\arguments{
\item{x}{A cohort table or cdm_table.}

\item{indexDate}{Date variable in \code{x} anchoring the observation window. Default: \code{"cohort_start_date"}.}

\item{censorDate}{Optional date variable in \code{x} to censor observation.}

\item{window}{A named or unnamed list of 2-element numeric vectors. Default: \code{list(c(-365, -1), c(0, 365))}.}

\item{settings}{Character vector of care settings to extract. Allowed values: \code{"inpatient"}, \code{"outpatient"}, \code{"emergency"}. Default: \code{c("inpatient", "outpatient", "emergency")}.}

\item{stratifySpecialty}{Logical; whether to partition visits by specialty. Default: \code{TRUE}.}

\item{gpSpecialtyConceptIds}{OMOP provider specialty concept IDs for General Practice. Default: \code{c(38004446L)}.}

\item{icuSpecialtyConceptIds}{OMOP provider specialty concept IDs for ICU stays. Default: \code{c(38004500L)}.}

\item{emergencySpecialtyConceptIds}{OMOP provider specialty concept IDs for Emergency Medicine. Default: \code{c(38004510L)}.}

\item{specialties}{Optional named list of integer vectors of OMOP specialty concept IDs for granular specialty breakdown. Default: \code{NULL}.}

\item{inpatientVisitConceptIds}{OMOP visit concept IDs for inpatient stays. Default: \code{c(9201L, 8717L, 581379L)}.}

\item{outpatientVisitConceptIds}{OMOP visit concept IDs for outpatient care. Default: \code{c(9202L, 581477L)}.}

\item{emergencyVisitConceptIds}{OMOP visit concept IDs for emergency care. Default: \code{c(9203L, 262L, 581478L)}.}

\item{icuConceptIds}{OMOP visit concept IDs for ICU stays. Default: \code{32037L}.}

\item{readmissions}{Logical; whether to compute 30-day and 90-day readmissions for inpatient stays. Default: \code{FALSE}.}

\item{gapDays}{Integer scalar >= 0. Maximum gap in days between contiguous stays to collapse into a single episode. Default: \code{1L}.}

\item{collapseGap}{Optional alias for \code{gapDays}. Default: \code{NULL}.}

\item{countBy}{Character scalar specifying count granularity: \code{"days"} to count distinct visit start dates, or \code{"records"} to count raw database rows. Default: \code{"days"}.}

\item{collapseOverlapping}{Logical; whether to collapse same-day and overlapping visits. If \code{FALSE}, forces \code{countBy = "records"}. Default: \code{TRUE}.}

\item{name}{Name of the new table in the write schema. If NULL, a temporary table is returned.}
}
\value{
The cohort table \code{x} with added multi-setting visit metric columns.
}
\description{
Enriches an OMOP cohort table with Healthcare Resource Utilization (HCRU) metrics
across Inpatient, Outpatient, and Emergency care settings in a single unified execution,
with full support for provider specialty stratification across domains.
}
