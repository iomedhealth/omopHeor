% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/addInpatients.R
\name{addInpatients}
\alias{addInpatients}
\alias{addHospitalizations}
\alias{addInpatient}
\alias{addIcuStays}
\title{Add Inpatient and ICU Hospitalization Metrics to a Cohort}
\usage{
addInpatients(
  x,
  indexDate = "cohort_start_date",
  censorDate = NULL,
  window = list(c(-365, -1), c(0, 365)),
  visitConceptIds = c(9201L, 8717L, 581379L),
  icuConceptIds = 32037L,
  icuSpecialtyConceptIds = c(38004500L),
  stratifySpecialty = FALSE,
  specialties = NULL,
  readmissions = FALSE,
  countBy = c("days", "records"),
  collapseOverlapping = TRUE,
  nameStyle = "{domain}_{metric}_{window_name}",
  name = NULL
)

addHospitalizations(
  x,
  indexDate = "cohort_start_date",
  censorDate = NULL,
  window = list(c(-365, -1), c(0, 365)),
  visitConceptIds = c(9201L, 8717L, 581379L),
  icuConceptIds = 32037L,
  icuSpecialtyConceptIds = c(38004500L),
  stratifySpecialty = FALSE,
  specialties = NULL,
  readmissions = FALSE,
  countBy = c("days", "records"),
  collapseOverlapping = TRUE,
  nameStyle = "{domain}_{metric}_{window_name}",
  name = NULL
)

addInpatient(
  x,
  indexDate = "cohort_start_date",
  censorDate = NULL,
  window = list(c(-365, -1), c(0, 365)),
  visitConceptIds = c(9201L, 8717L, 581379L),
  icuConceptIds = 32037L,
  icuSpecialtyConceptIds = c(38004500L),
  stratifySpecialty = FALSE,
  specialties = NULL,
  readmissions = FALSE,
  countBy = c("days", "records"),
  collapseOverlapping = TRUE,
  nameStyle = "{domain}_{metric}_{window_name}",
  name = NULL
)

addIcuStays(
  x,
  indexDate = "cohort_start_date",
  censorDate = NULL,
  window = list(c(-365, -1), c(0, 365)),
  icuConceptIds = 32037L,
  icuSpecialtyConceptIds = c(38004500L),
  countBy = c("days", "records"),
  collapseOverlapping = TRUE,
  nameStyle = "{domain}_{metric}_{window_name}",
  name = NULL
)
}
\arguments{
\item{x}{A cohort table or cdm_table.}

\item{indexDate}{Date variable in \code{x} anchoring the observation window. Default: \code{"cohort_start_date"}.}

\item{censorDate}{Optional date variable in \code{x} to censor observation.}

\item{window}{A named or unnamed list of 2-element numeric vectors. Default: \code{list(c(-365, -1), c(0, 365))}.}

\item{visitConceptIds}{OMOP visit concept IDs for general inpatient stays. Default: \code{c(9201L, 8717L, 581379L)}.}

\item{icuConceptIds}{OMOP visit concept IDs for ICU stays. Default: \code{32037L}.}

\item{icuSpecialtyConceptIds}{OMOP provider specialty concept IDs for ICU stays. Default: \code{c(38004500L)}.}

\item{stratifySpecialty}{Logical; whether to compute specialty breakdown. Default: \code{FALSE}.}

\item{specialties}{Optional named list of integer vectors of OMOP specialty concept IDs for granular specialty breakdown. Default: \code{NULL}.}

\item{readmissions}{Logical; whether to compute 30-day and 90-day readmissions. Default: \code{FALSE}.}

\item{countBy}{Character scalar specifying count granularity: \code{"days"} to collapse overlapping/contiguous stays into episodes, or \code{"records"} to count raw rows. Default: \code{"days"}.}

\item{collapseOverlapping}{Logical; whether to collapse overlapping and contiguous stays. If \code{FALSE}, forces \code{countBy = "records"}. Default: \code{TRUE}.}

\item{nameStyle}{Column naming pattern. Default: \code{"{domain}_{metric}_{window_name}"}.}

\item{name}{Name of the new table in the write schema. If NULL, a temporary table is returned.}
}
\value{
The cohort table \code{x} with added inpatient metric columns.
}
\description{
Add Inpatient and ICU Hospitalization Metrics to a Cohort
}
