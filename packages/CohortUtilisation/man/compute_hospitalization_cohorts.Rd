% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hospitalizations.R
\name{compute_hospitalization_cohorts}
\alias{compute_hospitalization_cohorts}
\alias{computeHospitalizationCohorts}
\title{Generate Hospitalization and Readmission Cohorts (Stage 1 & 2)}
\usage{
compute_hospitalization_cohorts(
  cdm,
  name,
  visit_concept_ids = c(9201L, 262L, 32037L, 581379L),
  readmission_window = 30L,
  collapse_gap = 1L,
  gap_days = NULL
)

computeHospitalizationCohorts(
  cdm,
  name,
  visitConceptIds = c(9201L, 262L, 581379L),
  icuConceptIds = 32037L,
  readmissionWindow = 30L,
  gapDays = 1L,
  collapseGap = NULL,
  readmission_window = NULL
)
}
\arguments{
\item{cdm}{A \code{cdm_reference} object.}

\item{name}{String specifying the cohort table name in the write schema.}

\item{visit_concept_ids}{Integer vector of OMOP visit concept IDs. Default: \code{c(9201L, 262L, 32037L, 581379L)}.}

\item{readmission_window}{Backward compatible snake_case parameter.}

\item{collapse_gap}{Maximum gap in days between contiguous stays to collapse into a single episode. Default: \code{1L}.}

\item{gap_days}{Optional alias for \code{collapse_gap}.}

\item{visitConceptIds}{Integer vector of OMOP visit concept IDs. Default: \code{c(9201L, 262L, 581379L)}.}

\item{icuConceptIds}{Integer vector of OMOP ICU visit concept IDs. Default: \code{32037L}.}

\item{readmissionWindow}{Maximum days between previous discharge and next admission. Default: 30.}

\item{gapDays}{Maximum gap in days between contiguous stays to collapse into a single episode. Default: \code{1L}.}

\item{collapseGap}{Optional alias for \code{gapDays}. Default: \code{NULL}.}
}
\value{
An \code{omopgenerics} cohort table with cohort definitions:
\itemize{
\item \code{1}: \code{hospitalization} (collapsed inpatient episodes)
\item \code{2}: \code{readmission} (episodes occurring within \code{readmission_window} days of prior discharge)
}
}
\description{
Extracts inpatient visits from \code{visit_occurrence}, collapses contiguous or
overlapping stays into discrete hospitalization episodes, and derives
readmission cohorts within a specified washout window.
}
\note{
For in-database Healthcare Resource Utilization (HCRU) characterization on existing study cohorts, prefer \code{\link{addInpatients}}.
}
