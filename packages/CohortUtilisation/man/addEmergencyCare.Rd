% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/addEmergencyCare.R
\name{addEmergencyCare}
\alias{addEmergencyCare}
\alias{addEmergency}
\alias{addEmergencyVisits}
\title{Add Emergency Care Utilization Metrics to a Cohort}
\usage{
addEmergencyCare(
  x,
  indexDate = "cohort_start_date",
  censorDate = NULL,
  window = list(c(-365, -1), c(0, 365)),
  emergencyVisitConceptIds = c(9203L, 262L, 581478L),
  emergencySpecialtyConceptIds = c(38004510L),
  stratifySpecialty = FALSE,
  specialties = NULL,
  countBy = c("days", "records"),
  collapseOverlapping = TRUE,
  nameStyle = "emergency_visits_{window_name}",
  name = NULL
)

addEmergency(
  x,
  indexDate = "cohort_start_date",
  censorDate = NULL,
  window = list(c(-365, -1), c(0, 365)),
  emergencyVisitConceptIds = c(9203L, 262L, 581478L),
  emergencySpecialtyConceptIds = c(38004510L),
  stratifySpecialty = FALSE,
  specialties = NULL,
  countBy = c("days", "records"),
  collapseOverlapping = TRUE,
  nameStyle = "emergency_visits_{window_name}",
  name = NULL
)

addEmergencyVisits(
  x,
  indexDate = "cohort_start_date",
  censorDate = NULL,
  window = list(c(-365, -1), c(0, 365)),
  emergencyVisitConceptIds = c(9203L, 262L, 581478L),
  emergencySpecialtyConceptIds = c(38004510L),
  stratifySpecialty = FALSE,
  specialties = NULL,
  countBy = c("days", "records"),
  collapseOverlapping = TRUE,
  nameStyle = "emergency_visits_{window_name}",
  name = NULL
)
}
\arguments{
\item{x}{A cohort table or cdm_table.}

\item{indexDate}{Date variable in \code{x} anchoring the observation window. Default: \code{"cohort_start_date"}.}

\item{censorDate}{Optional date variable in \code{x} to censor observation.}

\item{window}{A named or unnamed list of 2-element numeric vectors. Default: \code{list(c(-365, -1), c(0, 365))}.}

\item{emergencyVisitConceptIds}{OMOP visit concept IDs for emergency care. Default: \code{c(9203L, 262L, 581478L)}.}

\item{emergencySpecialtyConceptIds}{OMOP provider specialty concept IDs for emergency medicine. Default: \code{c(38004510L)}.}

\item{stratifySpecialty}{Logical; whether to compute specialty breakdown. Default: \code{FALSE}.}

\item{specialties}{Optional named list of integer vectors of OMOP specialty concept IDs for granular specialty breakdown. Default: \code{NULL}.}

\item{countBy}{Character scalar specifying count granularity: \code{"days"} to count distinct visit start dates/episodes, or \code{"records"} to count raw database rows. Default: \code{"days"}.}

\item{collapseOverlapping}{Logical; whether to collapse same-day and overlapping visits. If \code{FALSE}, forces \code{countBy = "records"}. Default: \code{TRUE}.}

\item{nameStyle}{Column naming pattern. Default: \code{"emergency_visits_{window_name}"}.}

\item{name}{Name of the new table in the write schema. If NULL, a temporary table is returned.}
}
\value{
The cohort table \code{x} with added emergency visit metric columns.
}
\description{
Identifies emergency encounters by querying \code{visit_occurrence} and \code{provider}
tables, capturing encounters with emergency visit concept IDs (e.g. 9203, 262, 581478)
as well as visits delivered by emergency medicine specialist providers (e.g. 38004510),
with optional granular specialty stratification.
}
